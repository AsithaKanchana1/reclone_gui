name: "Release"

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  # ════════════════════════════════════════════════════════════════
  # Tauri builds – Linux, Windows, macOS
  # ════════════════════════════════════════════════════════════════
  build-tauri:
    strategy:
      fail-fast: false
      matrix:
        include:
          # ── Linux x86_64 (AppImage + deb + rpm) ─────────────────
          - platform: ubuntu-22.04
            rust_target: x86_64-unknown-linux-gnu
            label: linux-x86_64
          # ── Windows x86_64 (MSI + NSIS exe) ─────────────────────
          - platform: windows-latest
            rust_target: x86_64-pc-windows-msvc
            label: windows-x86_64
          # ── macOS x86_64 ────────────────────────────────────────
          - platform: macos-13
            rust_target: x86_64-apple-darwin
            label: macos-x86_64
          # ── macOS ARM64 (Apple Silicon) ─────────────────────────
          - platform: macos-latest
            rust_target: aarch64-apple-darwin
            label: macos-aarch64

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ── System dependencies (Linux – Tauri v2 needs webkit2gtk-4.1) ──
      - name: Install Linux deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev

      # ── Node.js ───────────────────────────────────────────────
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install Node deps
        run: npm ci

      # ── Rust ──────────────────────────────────────────────────
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_target }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      # ── Build Tauri app (creates GitHub Release on first run) ─
      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "Reclone GUI ${{ github.ref_name }}"
          releaseBody: |
            ## Reclone GUI ${{ github.ref_name }}

            ### Downloads
            | Platform | Format |
            |----------|--------|
            | Linux x86_64 | AppImage, .deb, .rpm |
            | Windows x86_64 | .msi, NSIS .exe |
            | macOS x86_64 | .dmg |
            | macOS ARM64 | .dmg |

            > **Flatpak**: see the Flatpak build job artifacts or install from the manifest.

            See the [CHANGELOG](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) for details.
          releaseDraft: false
          prerelease: false

  # ════════════════════════════════════════════════════════════════
  # Flatpak build (Linux only)
  # ════════════════════════════════════════════════════════════════
  build-flatpak:
    runs-on: ubuntu-latest
    needs: build-tauri
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Flatpak + flatpak-builder
        run: |
          sudo apt-get update
          sudo apt-get install -y flatpak flatpak-builder
          sudo flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
          sudo flatpak install -y flathub org.gnome.Platform//46 org.gnome.Sdk//46 \
            org.freedesktop.Sdk.Extension.node20//24.08 \
            org.freedesktop.Sdk.Extension.rust-stable//24.08

      - name: Build Flatpak bundle
        run: |
          flatpak-builder --force-clean --repo=repo build-dir flatpak/com.asithakanchana.reclone-gui.json
          flatpak build-bundle repo reclone-gui.flatpak com.asithakanchana.reclone-gui

      - name: Upload Flatpak to release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: reclone-gui.flatpak
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
